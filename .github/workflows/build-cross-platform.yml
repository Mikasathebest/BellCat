name: Build cross-platform packages

on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build-macos:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build macOS app and DMG
        run: |
          chmod +x build-app.sh build-dmg.sh
          ./build-app.sh
          ./build-dmg.sh
          ditto -c -k --sequesterRsrc --keepParent BellCat.app BellCat-2.5.10-macOS-arm64.zip
      - uses: actions/upload-artifact@v4
        with:
          name: BellCat-macOS-arm64
          path: |
            BellCat-2.5.10-macOS-arm64.dmg
            BellCat-2.5.10-macOS-arm64.zip
      - name: Attach macOS packages to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            BellCat-2.5.10-macOS-arm64.dmg
            BellCat-2.5.10-macOS-arm64.zip

  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            artifact: BellCat-Windows-x64
            output: BellCat.exe
          - os: ubuntu-latest
            artifact: BellCat-Linux-x64
            output: BellCat
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install build dependencies
        run: pip install -r CrossPlatform/requirements-build.txt
      - name: Install Linux GUI dependencies
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y python3-tk libnotify-bin
      - name: Build BellCat
        run: pyinstaller --onefile --windowed --name BellCat --icon AppResources/BellCat.ico --add-data "AppResources/Ambience:Ambience" --add-data "AppResources/BellCatIcon-1024.png:." CrossPlatform/bellcat.py
      - name: Package Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path dist/BellCat.exe,CrossPlatform/README.md,LICENSE,THIRD_PARTY_NOTICES.md -DestinationPath BellCat-Windows-x64.zip
      - name: Install Inno Setup
        if: runner.os == 'Windows'
        shell: pwsh
        run: choco install innosetup --no-progress -y
      - name: Build Windows installer
        if: runner.os == 'Windows'
        shell: pwsh
        run: '& "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss'
      - name: Package Linux
        if: runner.os == 'Linux'
        run: tar -czf BellCat-Linux-x64.tar.gz -C dist BellCat -C ../CrossPlatform README.md -C .. LICENSE THIRD_PARTY_NOTICES.md
      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: |
            BellCat-Windows-x64.zip
            BellCat-2.5.10-Windows-x64-Setup.exe
            BellCat-Linux-x64.tar.gz
          if-no-files-found: ignore
      - name: Attach packages to release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            BellCat-Windows-x64.zip
            BellCat-2.5.10-Windows-x64-Setup.exe
            BellCat-Linux-x64.tar.gz
